# CLAUDE.md — MMORPG-with-IOCP : 3D 확장 프로젝트

이 문서는 이 프로젝트에서 Claude가 따라야 하는 **최상위 규칙**이다.
사용자의 개별 지시가 이 문서와 충돌하면 사용자 지시를 따르되, 충돌한다는 사실을 먼저 한 줄로 알린다.

---

## 0. 프로젝트 정체성 (가장 중요)

**이것은 서버 개발자 포트폴리오다. 게임이 아니다.**

- 평가자는 게임 서버 개발자 채용 담당자다. 그래픽 품질로 평가받지 않는다.
- 3D 전환, 마을, 상점, 퀘스트, 몬스터는 **모두 서버 기술을 증명하기 위한 수단**이다. 목적이 아니다.
- 각 기능이 증명해야 하는 서버 역량:

| 기능 | 증명하려는 서버 역량 |
|---|---|
| 3D 좌표계 전환 | 공간 분할(Sector/Zone) 재설계, 서버 권위 이동 검증 |
| Recast Navigation | 서버사이드 내비메시, 스레드별 쿼리 격리, 경로 재계산 억제 |
| 몬스터 3종 | 데이터 주도 AI 조합, 어그로 전파, Sector 기반 AI LOD |
| 마을(평화지역) | Zone 속성 기반 규칙 엔진, 서버 권위 판정 |
| 상점 / 인벤토리 | 아이템 복사 버그 방어, 원자적 트랜잭션, DB 비동기 영속화 |
| 퀘스트 | 이벤트 기반 상태머신, 상태 영속화, dirty flag flush |

**판단 기준:** 어떤 작업을 할지 애매할 때는 "이 작업이 위 표의 어떤 칸을 채우는가?"를 묻는다. 어떤 칸도 채우지 못하면 하지 않는다. 그리고 그 판단을 사용자에게 한 줄로 알린다.

---

## 1. Claude의 역할 경계 — 반드시 지킬 것

### 할 수 있는 것
- C++ 서버 코드 (IOCP 코어는 기존 것 재사용, 신규 시스템 설계·구현)
- Godot 4 GDScript / 에디터 툴 스크립트
- **월드 배치 데이터 생성** — 프로시저럴 배치 스크립트(Python)로 `world/*.json` 생성
- Blender Python 스크립트 (메시 변형·조합·LOD 생성·OBJ 병합 export)
- 셰이더(`.gdshader`), GPUParticles3D 설정, 트윈/타임라인 기반 이펙트 코드
- Recast/Detour 빌드 파이프라인 및 바인딩 코드
- 문서, 다이어그램 소스(Mermaid), 포트폴리오 문단 초안

### 할 수 없는 것 — 할 수 있다고 말하지 말 것
- **3D 메시·텍스처·머티리얼 이미지·스킨 애니메이션 클립을 만들 수 없다.** 이미지/모델 생성 능력이 없다.
- 에셋이 필요하면 다음 순서로 대응한다:
  1. 이미 받아둔 팩(§6) 안에서 재사용·재조합할 방법을 먼저 제안
  2. Blender 스크립트로 기존 메시를 변형(스케일·불리언·머티리얼 스왑·모듈 조합)해 변종을 만드는 방법 제안
  3. 그래도 없으면 **CC0 무료 출처와 구체적 검색어를 제시**하고 사용자에게 받아오기를 요청
  4. 임시로는 Godot 프리미티브(BoxMesh/CylinderMesh) + 단색 머티리얼로 **회색박스 프록시**를 코드로 배치
- **유료 에셋을 제안하지 않는다.** 사용자는 취업 준비 중이며 예산이 0이다. CC0 / name-your-own-price만 제안한다.
- 절대 금지: "에셋을 생성했습니다", "텍스처를 만들어 넣었습니다" 같은 허위 보고. 파일을 만들지 못했으면 만들지 못했다고 쓴다.

---

## 2. 기술 스택 (고정 — 임의 변경 금지)

```
서버      C++20, 기존 IOCP 코어 재사용, Windows
경로탐색  Recast & Detour (오프라인 빌드 / 런타임 쿼리)
몬스터 AI FSM (BehaviorTree 사용하지 않음 — §8 참조)
DB        기존 스택 유지 (변경 제안하지 말 것)
클라이언트 Godot 4.x (GDScript 기본, 성능 병목만 GDExtension 검토)
직렬화    기존 패킷 구조 유지 — 신규 패킷만 추가
툴체인    Python 3 (배치 생성기), Blender Python (에셋 가공)
```

- **기존 IOCP 네트워크 계층, Zone/Sector 스레딩 모델, TimerThread는 유지한다.** 재작성 제안 금지.
- 새 라이브러리 도입은 사용자 승인 후에만. 도입 시 이유·대안·제거 비용을 3줄로 제시.
- SFML 기반 기존 클라이언트는 삭제하지 않고 `legacy/`로 보존한다 (2D→3D 비교 자료).

---

## 3. 좌표계 및 단위 규약 (여기서 틀리면 전부 무너짐)

```
단위       1 unit = 1 meter
좌표계     오른손, Y-up, -Z가 forward (Godot 4 기본과 동일)
지면 평면  XZ. 높이는 Y.
서버 위치  struct Vec3 { float x, y, z; }   // y는 높이
```

- **Sector 분할은 XZ 평면 2D 격자로만 한다. Y는 분할하지 않는다.** 시야 처리에 Y 분할은 이득이 없고 복잡도만 늘린다.
- Zone 경계도 XZ 기준. 층이 겹치는 구조물은 같은 Zone에 둔다.
- Recast도 Y-up이므로 서버 좌표를 그대로 넘긴다. **좌표 변환 코드를 끼워넣지 말 것** — 버그의 90%가 여기서 난다.
- 회전은 서버에서 **yaw(Y축 회전) float 하나만** 관리한다. 쿼터니언을 서버에 넣지 않는다.
- 클라 표시용 보간·피치·롤은 클라이언트 전용. 서버에 올리지 않는다.

---

## 4. 월드 규모 (스코프 — 협상 대상 아님)

```
마을 (평화지역)   256 x 256,  Y 범위 0~64
필드 (전투)       512 x 512,  Y 범위 0~64
인던              128 x 128,  1개
```

- 타일 기반으로 확장 가능한 구조로 설계하되, **초기 빌드는 위 크기를 넘지 않는다.**
- 사용자가 더 넓은 월드를 요청하면: 네비메시 빌드 시간, 배치 작업량, 포트폴리오 기여도 0이라는 근거로 한 번 반대 의견을 제시한다. 그래도 원하면 따른다.
- 확장성은 **코드로 증명**한다 — 타일 스트리밍 인터페이스만 만들고 타일 수는 적게 유지.

---

## 5. 월드 배치는 "코드"가 아니라 "데이터"다

이 프로젝트의 핵심 설계 결정. 반드시 따를 것.

```
world/
  manifest.json          # 에셋 ID → 파일 경로, 콜리전 타입, 출처, 라이선스
  regions/
    town.json            # 배치 데이터 (Single Source of Truth)
    field_01.json
    dungeon_01.json
  build/
    town.obj             # 콜리전 메시 병합 결과 (Recast 입력)
    town.navmesh         # Detour 바이너리 (서버 로드)
```

배치 엔트리 스키마:

```json
{
  "asset": "prop_tree_pine_01",
  "pos": [128.0, 0.0, -64.5],
  "yaw": 1.5708,
  "scale": 1.0,
  "collision": "static_walkable | static_blocker | none",
  "nav": "include | exclude | area:water"
}
```

규칙:

- **Godot 씬 파일에 손으로 오브젝트를 배치하지 않는다.** `regions/*.json`만이 진실이고, Godot 임포터 스크립트가 이를 읽어 씬을 생성한다.
- **서버와 클라가 같은 JSON을 읽는다.** 콜리전 불일치(클라에선 벽, 서버에선 통과)가 원천적으로 발생하지 않는 구조를 만든다. 면접에서 반드시 설명할 포인트이므로 `docs/decisions/`에 기록한다.
- 배치 생성은 `tools/gen_layout.py`에 프로시저럴로 작성한다. **seed를 고정**해 재현 가능해야 한다.
- 생성기 요구사항: 도로/광장에 프롭 겹침 없음, 프롭 간 최소 간격 유지, NPC·스폰 지점 주변 반경 3m 비움, 지면 높이 스냅.

---

## 6. 에셋 (확정 — CC0만 사용)

전부 **CC0**(상업적 사용 가능, 출처 표기 의무 없음), **name your own price로 0원 다운로드**, **glTF 배포**.

| 팩 | 담당 | 출처 |
|---|---|---|
| KayKit Medieval Builder Pack | 마을(평화지역) 256² — 건물·담장·도로·물 타일 | kaylousberg.itch.io |
| KayKit Dungeon Pack | 인던 128² — 모듈형 벽/바닥/계단, 소품, 무기, 루팅 | kaylousberg.itch.io |
| KayKit Character Pack: Adventurers | 플레이어 + 상인 NPC (머리 교체 가능) | kaylousberg.itch.io |
| KayKit Skeletons | 몬스터 3종 전부 | kaylousberg.itch.io |
| KayKit Character Animations | 전 캐릭터 애니메이션 | kaylousberg.itch.io |
| Quaternius — Stylized Nature MegaKit | 필드 512² 식생·바위 | quaternius.com |

### 절대 규칙
- **`.glb`(glTF)만 사용한다. FBX 쓰지 않는다.** Godot 4는 glTF가 네이티브고 FBX는 별도 변환기를 거쳐 깨질 여지가 있다. 같은 팩 안의 glTF 폴더만 쓴다.
- **애니메이션은 KayKit Character Animations만 쓴다. Mixamo 쓰지 않는다.** KayKit 리그 전용이라 임포트하면 그냥 맞는다. 스타일라이즈 리그에 Mixamo를 리타깃하면 며칠이 날아간다.
- **출처를 늘리지 않는다.** KayKit이 기본이고, Quaternius는 자연물에만 쓴다. 이질감이 생기는 건 같은 카테고리를 섞을 때(KayKit 기사 + Quaternius 기사)이지 건물과 나무를 섞을 때가 아니다. 새 출처를 제안하려면 먼저 이유를 설명하고 승인을 받는다.
- 이질감 보정 우선순위: **머티리얼 통일 > 스케일 통일 > 메시 스타일**. `assets/materials/palette.tres` 한 세트로 머티리얼을 덮어쓰는 것을 먼저 시도한다.
- 명명 규약: `{category}_{name}_{variant}` — `prop_tree_pine_01`, `bld_shop_a`, `chr_skeleton_warrior`
- **모든 에셋의 출처·라이선스를 `world/manifest.json`에 기록한다.** 누락 금지.
- **에셋은 `assets/vendor/`에 두고 `.gitignore` 한다.** 저장소가 수백 MB로 불면 채용 담당자가 클론하지 않는다. `manifest.json`에 출처를, `README`에 다운로드 절차를 적는다.
- **상인 NPC 전용 모델은 없다.** Adventurers의 Mage에 머티리얼만 바꿔 쓰고 Idle 애니메이션만 재생한다. 상인 외형에 시간을 쓰지 않는다 — 정체성은 상점 UI와 트랜잭션 로그가 담당한다.
- **엘리트 몬스터 전용 모델도 만들지 않는다.** Skeletons 팩의 워리어에 스케일 1.4배 + 머티리얼 교체로 처리한다.
- 이펙트는 클라이언트 전용. 서버는 `effect_id`와 발생 좌표·대상만 보낸다. 서버가 파티클을 아는 순간 설계가 틀린 것이다.

---

## 7. Recast Navigation 규칙

**빌드는 오프라인, 쿼리는 런타임.**

```
regions/*.json → tools/build_collision.py → build/*.obj
              → tools/build_navmesh (RecastBuilder) → build/*.navmesh
              → 서버 시작 시 dtNavMesh 로드
```

- 런타임에 내비메시를 빌드하지 않는다. 바이너리를 로드한다.
- **`dtNavMesh`는 읽기 전용이므로 모든 Zone 스레드가 공유한다. `dtNavMeshQuery`는 스레드 안전하지 않으므로 Zone 스레드마다 하나씩 소유한다.** 이 규칙을 어기면 재현 불가능한 크래시가 난다. 코드에 주석으로 명시할 것.
- `dtCrowd`는 사용하지 않는다. 단일 스레드 전역 상태를 가정하므로 Zone 스레드 모델과 충돌한다. `findPath` + `findStraightPath` + 자체 스티어링으로 간다. 근거를 `docs/decisions/`에 기록.
- **경로 재계산 억제** — 다음 조건에서만 재계산한다:
  - FSM 상태 전이 시점
  - 목표가 N미터 이상 이동했을 때 (기본 3.0m)
  - 개체당 최소 재계산 간격 경과 (기본 500ms)
- **재계산 횟수를 카운터로 측정해 남긴다.** 기존 A* 병목 개선 서사(4천→1만6천)와 직접 연결되는 수치다.
- 클라이언트는 내비메시를 경로 디버그 시각화에만 쓴다. 이동 권위는 서버.

---

## 8. 몬스터 구성

### 원칙: 타입을 enum으로 나누지 않는다

`AGGRESSIVE`, `PEACEFUL`, `PATROL_ELITE` 같은 enum은 조합 폭발한다. **직교하는 속성의 조합으로 타입을 만든다.**

| 속성 | 값 |
|---|---|
| `aggression` | `passive` (선공 안 함) / `aggressive` (시야 내 선공) / `defensive` (피격 시에만 반격) |
| `on_damaged` | `fight` / `flee` / `call_help` |
| `idle_movement` | `stationary` / `wander` / `patrol` (웨이포인트) |
| `assist` | `none` / `family` (같은 계열 피격 시 참전) |
| `combat_range` | `melee` / `ranged` |
| `leash_radius` | 스폰 지점 이탈 한계. 초과 시 귀환 |
| `faction` | 진영. 적대 관계는 팩션 매트릭스가 결정 |

**목표: JSON만 추가하면 새 몬스터가 생기고, C++ 재컴파일이 없다.** 이것이 포트폴리오에 쓸 문장이다.

```json
{
  "id": "skel_warrior_01",
  "name": "스켈레톤 워리어",
  "model": "chr_skeleton_warrior",
  "faction": "undead",
  "stats": { "hp": 220, "atk": 28, "move_speed": 3.6 },
  "ai": {
    "aggression": "aggressive",
    "aggro_radius": 10.0,
    "on_damaged": "fight",
    "idle_movement": "wander",
    "assist": "none",
    "combat_range": "melee",
    "leash_radius": 20.0,
    "repath": { "target_move_threshold": 3.0, "min_interval_ms": 500 },
    "tick_hz": { "idle": 1, "alert": 5, "combat": 10 }
  },
  "skills": ["slash"],
  "drops": "table_undead_mid"
}
```

### 몬스터 3종 (확정 — 늘리지 않는다)

| 몬스터 | 속성 조합 | 증명하는 서버 기술 |
|---|---|---|
| **스켈레톤 워리어** | `aggressive` + `melee` + `wander` + leash 20m | 기본 추격, **리쉬 귀환**, 경로 재계산 억제 검증 |
| **스켈레톤 미니언 무리** | `passive` + `assist: family` + `wander` | **어그로 전파** — 한 마리 피격 시 주변 동족 참전. 팩션 테이블 |
| **스켈레톤 엘리트** | `aggressive` + `patrol` + leash 40m | **웨이포인트 순찰 + 어그로 테이블(위협 수치 누적)** |

- 미니언이 `passive`(선공 안 함)이므로 **피스풀 속성 축은 3종 안에서 그대로 증명된다.** 별도 평화 몬스터를 추가하지 않는다.
- 도주(`on_damaged: flee`)는 속성 테이블에 정의만 남겨두고 **구현하지 않는다.** 스코프 밖이다.
- 원거리(`combat_range: ranged`)도 정의만 남기고 구현하지 않는다.
- 4번째 몬스터를 추가하고 싶어지면: 위 표의 "증명하는 서버 기술" 칸에 새로 채울 것이 있는지 먼저 확인한다. 없으면 추가하지 않는다.

### FSM으로 간다. BehaviorTree 쓰지 않는다

`Idle → Alert → Chase → Combat → Return → Dead` FSM으로 충분하다.

- 몬스터 3종 규모에서 BT의 이점(노드 재조합)은 나오지 않고 디버깅 난이도와 틱 비용만 올라간다.
- **이 결정을 `docs/decisions/`에 반드시 기록한다.** 사용자는 현업에서 BT 기반 콘텐츠 시스템을 다뤄봤다. "BT를 알고 있으나 이 규모에서는 FSM이 디버깅과 틱 비용 면에서 유리하다고 판단해 채택하지 않았다"는 서술은 강한 신호다. BT는 "확장 시 고려" 항목으로 남긴다.

### 틱 비용 관리 — 여기가 실제 평가 지점

- **Sector 기반 AI LOD** — 플레이어가 없는 Sector의 몬스터는 틱을 건너뛴다. **기존 Sector 설계를 그대로 재활용한다. 새로 만들지 않는다.**
- **상태별 틱 주기 차등** — Idle 1Hz / Alert 5Hz / Combat 10Hz. JSON의 `tick_hz`로 데이터화한다.
- **인지(perception) 검사** — 매 틱 전체 순회 금지. 자기 Sector + 인접 Sector로 후보를 좁히고, Alert 진입 전에는 저주기로만 검사한다.
- **스폰/디스폰은 오브젝트 풀링.** 리스폰 타이머는 **기존 TimerThread에 예약**한다.
- 스폰 포인트와 순찰 웨이포인트는 `world/regions/*.json`에 넣는다. 코드에 하드코딩하지 않는다.

---

## 9. 서버 권위 규칙 (절대)

- 클라이언트가 보내는 것은 **입력(이동 방향, 대상 지정, 상호작용 요청)**뿐이다. 결과 좌표를 신뢰하지 않는다.
- 이동 검증: 직전 위치와의 거리 / 경과 시간 → 최대 속도 초과 시 서버 좌표로 보정 패킷 전송. 내비메시 폴리곤 밖이면 거부.
- 평화지역: Zone에 `flags: { pvp: false, spawn: false }`. 판정은 **서버에서만**. 클라 UI 표시는 부가물.

### 상점 / 인벤토리 (아이템 복사 방어 — 최우선 관심사)
- 인벤토리 연산은 **해당 오브젝트를 소유한 Zone 스레드에서만** 실행한다. 락을 추가하지 않는다. 기존 Zone 액터 모델의 강점을 그대로 활용한다.
- 모든 상점 요청에 **클라 시퀀스 번호**를 붙이고 서버가 마지막 처리 번호를 기억해 중복 요청을 무시한다(idempotency).
- 구매 처리 순서: 재화 차감 → 슬롯 확보 → 아이템 지급. 중간 실패 시 **메모리 상태를 롤백**한다. 롤백 경로를 반드시 구현하고 테스트한다.
- DB는 저널이다. 메모리가 권위, DB는 비동기 flush. `UPDATE ... WHERE quantity >= ?` 형태로 쓰고 **affected rows를 반드시 확인**한다.
- **의도적으로 다음 공격 시나리오를 테스트 코드로 작성한다:** 동시 이중 구매, 구매 중 로그아웃, 인벤토리 만재 상태 구매, 시퀀스 번호 재전송. 이 테스트가 포트폴리오의 핵심 자료다.

### 퀘스트
- 데이터 주도: `data/quests/*.json` (조건, 목표, 보상). 코드에 퀘스트를 하드코딩하지 않는다.
- 이벤트 버스: `OnMonsterKilled`, `OnItemAcquired`, `OnNpcTalked` → 구독 중인 퀘스트 인스턴스만 갱신. 전체 순회 금지.
- 상태머신: `NotStarted → InProgress → Completable → Completed`. 전이는 서버만.
- 영속화: dirty flag + 주기적 flush + 로그아웃 시 강제 flush. 매 진행마다 DB 쓰기 금지.

---

## 10. 작업 프로토콜

### 세션 시작 시
1. 이 문서를 읽는다.
2. `docs/STATUS.md`를 읽어 현재 진행 지점을 파악한다.
3. 관련 `docs/decisions/`를 읽는다. **이미 내린 결정을 다시 논의하지 않는다.**

### 작업 방식
- 코드를 쓰기 전에 **무엇을 어디에 만들지 5줄 이내로 먼저 제시하고 동의를 받는다.**
- **한 번에 하나의 기능.** 여러 시스템을 동시에 건드리지 않는다.
- 기존 코드를 고칠 때는 최소 변경. "이왕 하는 김에" 리팩토링 금지.
- 기존 코드의 스타일(명명, 헤더 구성, 에러 처리)을 따른다. 새 스타일을 도입하지 않는다.
- 작업 종료 시 `docs/STATUS.md`를 갱신한다.

### 커밋
```
feat(nav): Recast 내비메시 로더 및 Zone별 쿼리 격리
feat(ai): 스켈레톤 워리어 FSM 및 리쉬 귀환
fix(shop): 동시 구매 시 재화 이중 차감 수정
docs(decisions): BehaviorTree 미채택 근거 기록
```

### 금지
- 기술 스택 임의 변경 / 라이브러리 무단 추가
- 유료 에셋 제안
- 몬스터 종류, 월드 크기, 에셋 출처를 승인 없이 늘리는 것
- 검증하지 않은 성능 수치를 문서나 코드 주석에 쓰는 것
- 동작 확인 없이 "완료"라고 보고하는 것
- 광범위한 리팩토링, 미사용 코드 대량 삭제
- 에셋을 만들었다는 허위 보고

---

## 11. 문서화 의무 (서류 통과와 직결)

**기능을 만들 때마다 즉시 문서를 남긴다. 나중에 몰아서 쓰지 않는다.**

```
docs/
  STATUS.md                 # 진행 상황 (매 세션 갱신)
  decisions/
    001-coordinate-system.md
    002-world-data-sot.md
    003-navmesh-thread-model.md
    004-fsm-over-behaviortree.md
    005-monster-attribute-composition.md
    ...
  portfolio/
    drafts/                 # PDF에 들어갈 문단 초안
    metrics.md              # 측정값 원본 (측정 방법·환경·날짜 필수)
    diagrams/               # Mermaid 소스
```

의사결정 문서 형식 (짧게, 5개 항목):

```markdown
# 004 — FSM over BehaviorTree
## 문제        무엇이 문제였는가
## 선택        무엇을 택했는가
## 대안        검토했고 버린 것과 그 이유
## 결과        수치 또는 관찰된 변화
## 남은 한계   알고 있는 미해결 지점
```

- 기능 완료 시 `docs/portfolio/drafts/`에 **면접관이 읽을 문단을 바로 초안으로** 쓴다. 코드 설명이 아니라 "어떤 문제를 어떻게 풀었는가" 서사로 쓴다.
- 모든 수치는 `metrics.md`에 측정 방법·환경·날짜를 함께 기록. **근거 없는 숫자를 절대 만들지 않는다.**
- 다이어그램은 Mermaid 소스로 관리한다 (수정 가능해야 함).
- 포트폴리오 PDF 형식: **10페이지 이상, 내용을 여러 페이지에 분산, 텍스트 + 다이어그램 + 스크린샷 혼합.**

---

## 12. 측정 계획

측정은 **기존 STRESS_TEST 툴을 3D로 확장해 재사용한다.** 새 부하 도구를 만들지 않는다.
측정 방법도 기존 방식을 유지한다 — 봇을 점진적으로 붙이며 CPU 사용률을 관찰, 튀면 속도를 줄이고 안정화되면 다시 올린다.

Week 2 종료 시 3단 비교:

```
A. 단일 몬스터 타입 + A*            (기존 상태)
B. 몬스터 3종 + Recast               (AI LOD 없음)
C. 몬스터 3종 + Recast + Sector AI LOD (최종)
```

**B를 반드시 측정한다. B가 나빠야 C의 의미가 생긴다.**
"개선했다"보다 "이렇게 하면 나빠지는 것을 확인하고 이렇게 고쳤다"가 훨씬 강한 서술이다.

함께 기록할 카운터: 경로 재계산 횟수/초, 틱당 AI 처리 개체 수, 스킵된 Sector 수, 트랜잭션 실패 수.

---

## 13. 완료 정의 (DoD)

기능은 아래를 전부 만족할 때만 "완료"다.

- [ ] 서버가 크래시 없이 동작 (기존 STRESS_TEST 봇으로 확인)
- [ ] 비정상 경로 처리 — 잘못된 패킷, 중간 접속 종료, 경계값
- [ ] 관련 카운터/로그 추가
- [ ] `docs/decisions/`에 결정 기록
- [ ] `docs/portfolio/drafts/`에 문단 초안
- [ ] `docs/STATUS.md` 갱신

---

## 14. 4주 계획

### Week 1 — 3D 기반 전환 (아트 없음, 전부 회색박스)
- `Vec3` 도입, Sector/Zone을 XZ 격자로 재편
- `world/regions/*.json` 스키마 확정 + `tools/gen_layout.py`
- Godot 임포터 스크립트 (JSON → 씬 생성)
- 서버 권위 이동 + 클라 보간, 시야 동기화 3D 동작
- **주말 기준: 회색박스 필드에서 캐릭터가 움직이고 서버가 검증한다**

### Week 2 — Recast + 몬스터 3종
- 콜리전 병합(OBJ) → 내비메시 빌드 파이프라인
- 서버 `dtNavMesh` 공유 + Zone별 `dtNavMeshQuery`
- 몬스터 스펙 JSON 로더 + FSM 골격
- **스켈레톤 워리어부터** 구현(추격 + 리쉬) → 미니언(어그로 전파) → 엘리트(순찰 + 어그로 테이블)
- Sector AI LOD + 경로 재계산 억제 + 카운터
- **A/B/C 3단 부하 측정 및 기록**

### Week 3 — 마을 / 상점 / 퀘스트
- 마을 Zone + 평화지역 플래그, 상인 NPC 상호작용
- 인벤토리 / 상점 트랜잭션 + **공격 시나리오 테스트 4종**
- 퀘스트 이벤트 버스 + 상태머신 + 영속화

### Week 4 — 아트 입히기 + 문서
- KayKit / Quaternius 에셋 적용, 머티리얼 팔레트 통일
- 필수 이펙트 3종만 (타격, 스킬 캐스팅, 아이템 획득)
- 부하 테스트 최종 측정
- **포트폴리오 PDF 갱신**

### 주간 원칙
- 금요일마다 **현재 상태로 제출 가능한지** 점검한다. 언제 지원할지 모르므로 항상 제출 가능한 상태를 유지한다.
- 일정이 밀리면 **아트(Week 4)를 먼저 깎는다.** 서버 기능(Week 2~3)은 깎지 않는다.
- 몬스터 순서를 지킨다: 워리어 → 미니언 → 엘리트. 워리어 FSM이 나머지의 골격이다.

---

## 15. 사용자에 대해

- C++ 게임 서버 경력 1년. IOCP, 멀티스레딩, 락 설계, 저수준 메모리 관리에 익숙하다. **기초 설명은 생략하고 바로 설계 논의로 들어간다.**
- 현업에서 **BehaviorTree 기반 콘텐츠 시스템, PvP/PvE 스킬 개발** 경험이 있다. 몬스터 AI 논의에서 이 경험을 전제로 대화한다.
- Godot / 3D 렌더링 / Blender는 처음이다. **이쪽은 단계별로 구체적으로 안내한다** (메뉴 경로, 스크립트 전문 포함).
- **예산 0원.** 유료 에셋·유료 도구를 제안하지 않는다.
- 현재 취업 준비 중이며 서류 단계에서 떨어지고 있다. CKA 준비와 병행한다. **시간이 곧 비용이다. 완벽함보다 제출 가능한 상태를 우선한다.**
- 장기적으로 DevOps/인프라 방향으로 전환 중이다. 컨테이너·CI·모니터링을 붙일 여지가 보이면 **한 줄로만 제안**하고 이번 4주 스코프에 넣지 않는다.
- 스코프가 커지려는 조짐이 보이면 **즉시 지적한다.** 원하는 것을 다 들어주는 것보다 1개월 안에 끝나게 하는 것이 도움이 된다.
